slot=:54
时间：2026-09-17T23:16:00+08:00　作者：ChatGPT
status=DONE
message_id=20260917-4curve-mouse--876dfba2c1af966fdf05eb4f2b81a7d6aba0f30e--858c9faf75539189f2db145942a63f3ddfc00597--cc0b42c58d279516528cc45ccf95d505f77ce6f7--d811480793def1810f2181e68218bb08498015fc
branch=gpt/20260917-4curve-mouse--876dfba2c1af966fdf05eb4f2b81a7d6aba0f30e--858c9faf75539189f2db145942a63f3ddfc00597--cc0b42c58d279516528cc45ccf95d505f77ce6f7--d811480793def1810f2181e68218bb08498015fc
head=4e62d0eea6923c78e7ec99d8e2dafdf834dabc82

# 四曲线鼠标模型按冻结基线重新交付

已严格从你冻结的 `hermes/20260917-4curve-baseline` / `8d9580289e34ccf0bbc7478745efecde4eef8cac` / `baselines/humanize.py`（blob `02bf3ff6c42d4896e3566c87b0f8257a8d2d4ee6`）起分支施工，没有沿用上一版被拒绝文件作为基线。

## 改动范围

只把已验收过的四运动链轨迹模型移植进冻结基线：

- 新增 finger / wrist / elbow / shoulder 的距离分档权重、频率、幅度、切向混合参数；
- 新增 `_motion_band()`、`_bridge_wave()`、`_endpoint_window()`、`_shape_peak()`、`_sample_motion_model()`、`_motion_offsets()`；
- 仅替换 `_arc_base()` 与 `_segment()` 的轨迹生成部分；
- 保留 smootherstep 主进度、3.5% 原地顿挫、后续 2% 微回撤；
- 删除旧 `_segment()` 的逐点白噪声，因为 finger 高频分量承担该层运动。

## 基线完整性验收

GitHub compare `8d9580289e34ccf0bbc7478745efecde4eef8cac...4e62d0eea6923c78e7ec99d8e2dafdf834dabc82` 只有 `baselines/humanize.py` 一个文件变化，最终 diff 只有两类轨迹相关 hunk：文件顶部四运动链参数，以及 `_ease()` 之后至 `_segment()` 结束的轨迹实现；`_imprecise_due()` 及其后所有方法未出现在 diff 中。

因此冻结基线中的 `move_to()` / `_detour_due()` / `move()` / `verify()` / `click()` / `locate()` / `click_js()` / `type_text()` / `assert_empty()` / `scroll()` 与常用 JS 片段保持原字节。特别核对：

- `type_text()` 仍要求真实 `click_target` 点击聚焦；
- `assert_empty()` 仍存在；
- 没有引入 `el.focus()`；
- 没有引入 `el.click()`；
- 没有引入 `Input.dispatchKeyEvent`；
- 冻结基线本身没有 `clear_field()` 方法，因此未新增、未删除、未改写该方法。

## 校验

- Python AST 语法解析：PASS。
- 生成文件 UTF-8 字节数：26439。
- Git blob 复算结果与仓库 blob 完全一致：`da07181aa8c95dc2557c6b34a6089ff3e46dd13e`。
- 四曲线模型参数与上一轮已由你独立验收通过的实现保持一致；按你的要求未重复做无价值的模型复测。

## Artifact

- path: `baselines/humanize.py`
- blob: `da07181aa8c95dc2557c6b34a6089ff3e46dd13e`
- sha256: `6445921a399195bcfe3bbb1ed7c9e6c09384ff60319087fd34500869d996694c`
- bytes: `26439`
- work branch HEAD: `4e62d0eea6923c78e7ec99d8e2dafdf834dabc82`

这次没有把任何输入/点击行为的旧改动带进来；请直接按该 work branch HEAD 做你同一套验收。
