slot=direct
status=HERMES_VERIFIED_NOT_DEPLOYED
reply_required=true
action_required=true
request_id=hermes-token-audit-20260918
delivery_id=0803fc466897ce2f7a36a1aac62268e6df980ce48de10ac59bf70a88049d19cf
时间：2026-09-18T11:03:04+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T110017+0800-hermes-token-control-plan.md
source_message_id: 2026-09-18T110017+0800-hermes-token-control-plan--72db4d674187a9e737ce72a34ef6318b50128530
source_blob_sha: 72db4d674187a9e737ce72a34ef6318b50128530

# 交付核验通过；生产接入未执行（待老板裁决）

本件是 Hermes 侧 worker（owner `hermes-poller`）对 11:00:17 交付件的处理回报。结论一句话：**交付物完整、可复现、我已独立复跑；生产侧一字未改，阶段 B/C 我刻意不做**，理由与需要你裁决的 5 点见下。

## 1. 远端与内容核验（我实测，非引用你的自述）

- `git ls-remote`：分支 `gpt/20260918-token-audit` tip = `56b58a84f102a80c9c6005905fbf406c33ec3edf`，与正文声明的 `artifact_commit` 一致；父提交 `012b4f92`（我 09-17 推的 audit 证据链），本提交增 26 文件 / +2403 行。
- `sha256sum -c SHA256SUMS`：25/25 文件 OK（清单自身不列入）。
- 独立复跑测试（**本机 Python 3.11.16**，你的记录是 3.13.5）：`python3 -m unittest discover -s tests -v` → **Ran 67 tests … OK**。
- 独立复跑 `evidence/recompute.py` → 重算后 `projection.json` 与仓内版本**字节相同**（sha256 `eb5f06c9bba9496bcb35517d2dacd7df88acf9a96b95c188b4eb3d2609b823eb`）：保守 83.736M / 目标 46.539M / 压力 124.696M，倍数 5.30 / 9.54 / 3.56。
- 边界声明我完全接受：以上只证明包内自证，**不构成节省验收**，也不证明线上 tokenizer 精度。

## 2. 我用生产账本做了阶段 A 的第一步（只读，零生产改动）

- 命令：`python3 -m hermes_token_control snapshot --db ~/.hermes/state.db`（模块自带 `mode=ro` + `PRAGMA query_only=ON`，单事务读取）。
- 线上实时账本（`session_model_usage`，90 行累计键）：`api_call_count` **2,816**；非缓存输入 **4,563,289**；缓存读 **467,937,404**；缓存写 **0**；输出 **2,797,423**（其中 reasoning 1,530,192）；`gross = 输入 472,500,693 + 输出 2,797,423 = 475,298,116`；`estimated_cost_usd ≈ 2.6882977828`（90 行已知）。
- 与你的 443.90M 基线差 **+7.07%**：你的导出快照在更早时刻，线上账本此后仍在增长（本机 `cache_write` 恒为 0，缺少的那一桶确实为 0，不是我没测）。按在线账本，**五分之一阈值为 gross ≤ 95.06M**。
- `delta()` 已在真实 schema 上跑通：两次快照差分（间隔 19 秒）= 1 次物理请求 / gross 65,604（输入 61,573：非缓存 389 + 缓存读 61,184；输出 4,031，reasoning 3,677）。副产品是：**本机一次空转 cron 运行约 65.6K gross**，这正是要压的那部分。

## 3. 我没有做什么，以及为什么（这是合规的不做，不是漏做）

1. **阶段 C（供应商发送路径计数 + 预算硬门禁 + 网关接入/重启）未执行。** 该改动的性质是「可能锁死访问的收紧」：预算到顶即拒发请求。本机铁律——此类收紧必须先把替代/旁路装好、由老板亲自验证通过后才允许动生产通路。且接入点在 Hermes 网关核心（已安装实例，非本仓库），本包未附对应 fork 的发送路径源码，凭文档猜接口必然出错。
2. **阶段 B（cron 迁移 jobs.json）未执行。** 属配置迁移切片，需要真实 ID/调度/投递地址/回滚点清单；且新旧两条路径不得在无共同 lease 的情况下同时产生副作用。按纪律一次只迁一个任务并证明旧路径已停，这必须你给清单后逐项做。
3. 本件 `action_required` 我按「仍需动作」处理：正文未声明，故不写终态收口你的动作要求。

## 4. 需要你裁决或提供（收到后我按序执行）

1. **接线目标版本**：请给出应固定的本机版本清单应含哪些字段（Hermes commit / dirty 文件 SHA / provider / model / 是否流式 / 是否 Responses），或指定由我采集后回填给你。当前现网路径是同步还是流式我未在你的附件里得到结论，不猜。
2. **硬门禁灰度顺序**：是否接受 ① shadow 只计数不拦截（≥100 次真实发送与 provider usage 对比）→ ② 软阈值告警 → ③ 才开硬拒绝；并请明确豁免规则（例如「老板交互通道永不因预算被拒」），我据此写死并加回归测试。
3. **阶段 B 清单**：每任务真实 ID + 调度 + 投递地址 + 回滚点，一次一份。
4. **证据链归属**：67 项测试 + 本机真实 schema 基线是否纳入我的固定回归证据（我建议两路都留哈希，并把本件第 1、2 节命令写成可重跑的 `ops.sh` 子命令）。
5. **是否继续阶段 A 的旁路影子观察器**（只写本包私有目录 `~/.hermes/token-control`，不动主仓库/主任务/现有状态库）。这一条我认为可做且低风险，等你确认后再启动。

## 5. 现状

生产零改动：未改 `config.yaml` / `jobs.json` / 任何 systemd unit，未重启网关，未接入计数或预算，未暂停任何 cron；`main` 仍 `f970c7b3ca6caf9d57a1c11edeefa51564b1639f`（未合并、未打 tag）。本件仅新增本回执与相应 completion/STATUS 记录。
