# V4.1 房间后端：生产安装完成，后端 healthy，线上 HTML 未切

时间：2026-09-18T17:55:28+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T175200+0800-campfire-v41-final-backend-deploy-go.md
source_message_id: 2026-09-18T175200+0800-campfire-v41-final-backend-deploy-go--bbd875bc643dbc2f9392e0014627ef3948ee191d
source_blob_sha: bbd875bc643dbc2f9392e0014627ef3948ee191d
action_required: false
reply_required: false

## 0. 门禁核对（先只读确认，再动手）

- `main` = `9ea66b6bbd499a2c1c71aa5d244e7fa0b52ba29a`，其 tree = `3efe674627e7b1a6ca9ceec24f1fad9b0a653f89` ✅ 与消息一致。
- 分支 SHA `914b9643f08c5d53c9bcc535dc4d798322f8fc49` 的 tree 同为 `3efe6746…` ✅。
- final quality run `35331452282`：`Campfire Rooms Quality`，status completed，conclusion **success**，head_sha `914b9643…`，head_branch `feat/campfire-v4.1-rooms-20260918` ✅。
- 资源租约：`resource:caddy-config` fence=1、`resource:production-site` fence=3，按 canonical key 升序获取、逆序释放（17:54:06 / 17:54:08 取得 → 17:55:30 / 17:55:32 释放）。全部生产动作在 PID1 下执行（`sudo systemd-run --wait --collect`），未触碰、未重启 Hermes gateway。

## 1. 安装（exact main，不可变 release）

| 项 | 值 |
|---|---|
| release 路径 | `/opt/campfire-rooms/releases/9ea66b6bbd499a2c1c71aa5d244e7fa0b52ba29a`（230 个文件，来自 `git archive <main SHA> projects/campfire-kitchen`） |
| current | `/opt/campfire-rooms/current` → 上述 release（`mv -Tf` 原子切换），root:root，`chmod -R u=rwX,go=rX,go-w` |
| venv | release 内 `.venv`，`/usr/bin/python3.12`（Python 3.12.3） |
| pip pinned | `anyio==4.13.0  certifi==2026.7.22  click==8.5.0  h11==0.16.0  httpcore==1.0.9  httpx==0.28.1  idna==3.20  starlette==0.50.0  typing_extensions==4.16.0  uvicorn==0.48.0`（`pip freeze` 存 `/opt/campfire-rooms/freeze-9ea66b6b….txt`；四条 pinned 需求逐字满足） |
| 系统用户 | `campfire-rooms` uid 994 / gid 985，shell `/usr/sbin/nologin`，无 home 创建 |
| DB | systemd `StateDirectory=campfire-rooms` → `/var/lib/campfire-rooms/rooms.sqlite3`（campfire-rooms 0600，schema 自动初始化 version=1） |
| unit 安装 | `/etc/systemd/system/campfire-rooms.service` sha256 `7e6177ea7b0be71cf0d3cc9f91f87dbb07a8cf72d1f244510049edd93eeb98b7`；`campfire-room-archive.service` sha256 `e1d02656ef4ebe00611e6f1842a4122d6221617b06116965e53376b30540725c`；`campfire-room-archive.timer` sha256 `872ec0e17942aa855f216923e9dc36b8129dbe11a28c08372a35d2eff5f56e54` |
| ROOMS_ORIGINS | `https://furrypant.com,https://www.furrypant.com`（unit 第 13 行，实测进程 environ 一致；非白名单 `Host: evil.example` → 403） |

补充事实：本机原先缺 `python3.12-venv`（`python3.12 -m venv` 直接失败），已 `apt-get install python3.12-venv`（3.12.3-1ubuntu0.17）后建 venv。

## 2. 归档凭据隔离

- `/etc/campfire-rooms/github_token`：root:root **0400**，93 字节（仅长度，无明文）；指纹 sha256 前 12 位 `330fa449c874`。
- 只经 archive oneshot 的 `LoadCredential=github_token:…` 注入；API unit 内 `LoadCredential` 计数 = 0。
- 隔离实测：`setpriv --reuid=campfire-rooms … cat /etc/campfire-rooms/github_token` → **Permission denied**（启动前后各一次）；API 进程（PID 851882）environ 中 `token|secret|password|credential|github` 关键词计数 = 0，`/proc/<pid>/fd` 中指向 `/etc/campfire-rooms` 的 fd 数 = 0。
- 明文未出现在任何命令回显、日志或 chat：脚本以变量直接写文件（无 `set -x`），回执只给长度与 sha256 前缀。

## 3. 真实 GitHub archive probe（未用替身）

- 运行一次 `campfire-room-archive.service`：`ExecMainCode=0 / ExecMainStatus=0`；journal 显示 `GET git/ref/heads/campfire-room-archives 200` → `GET contents/system/archive-probe-v1.json 404` → `PUT … 201`。
- 远端核对（`campfire-room-archives`）：路径 `system/archive-probe-v1.json`，**blob_sha `3538dd82a81f8a4c85f1ea4a67660d3414736b15`**，**commit `621c038cafa3d5a2ad320890a81f1838e5f45731`**（message `archive: closed campfire room archive-probe-v1`），54 字节，内容逐字节等于 `{"application":"campfire-room-archives","protocol":1}\n`（自算 git blob 与 API 返回值一致）。
- 随后 `archiveReady=true`（health 已含）；timer enable+active，下次触发 2026-09-18T17:58:57+08:00。

## 4. Caddy（互斥 handle 结构）

- 以 repo `deploy/rooms/caddy-route.caddy` 为目标结构，只替换 `furrypant.com, www.furrypant.com` site block；顶部 ACLI import 与 `:80` 兜底段原样保留（脚本内置断言）。
- 新 `/etc/caddy/Caddyfile` sha256 **`e497000dbbe3abdd3cfe2723fae0e0a7354d3ef2a0ff0d74bff8eba9b51a059e`**；改动前备份 `/etc/caddy/Caddyfile.bak-rooms-20260918-175419`（sha256 `97807a6ee810ff6535b0d8acdd7063b7787daeac65292e7ef78a9cbf4b315f71`，与改动前线上值一致）。
- `caddy fmt --diff` 只报末尾空行一处空白差异（无结构变化，未写回）；`caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` → **Valid configuration**；`systemctl reload caddy` → active，日志 `load complete`。
- 结构要点：`@campfire_rooms path /api/rooms /api/rooms/*` → `handle` 内 API `reverse_proxy 127.0.0.1:8914` + `Cache-Control "no-store"`，静态 `handle` → 原 webroot + `file_server` + `Cache-Control "no-cache"`，两侧均保留 nosniff / referrer-policy。旧的全局 `no-cache` 不再污染 API 响应（见验收 5）。

## 5. 验收（全项实测）

| 检查 | 结果 |
|---|---|
| loopback 直连 8914 `Host: furrypant.com` `/api/rooms/health` | 200，`cache-control: no-store`，`{"protocol":1,"enabled":true,"archiveReady":true}` |
| loopback 经 Caddy https（`--resolve furrypant.com:443:127.0.0.1`） | 200，`no-store`，同上 body |
| loopback 经 Caddy https www | 200，同上 body |
| **公网 apex** `https://furrypant.com/api/rooms/health` | HTTP/2 200，`cache-control: no-store`，`{"protocol":1,"enabled":true,"archiveReady":true}` |
| **公网 www** `https://www.furrypant.com/api/rooms/health` | HTTP/2 200，同上（旧候选的 www 403 问题已不复现） |
| 静态响应缓存头 | `Cache-Control: no-cache` + `X-Content-Type-Options: nosniff` + `Referrer-Policy: no-referrer` |
| 线上 V4.0 首页未变 | apex loopback / apex 公网 / www loopback 三处均 200、**7492813 字节、sha256 `97e85084999a0ebe9e73cd28a93b45328308babc1d41a0b766f14f91de2595fc`**（与部署前一致） |
| 未知静态路径 | loopback https 404、公网 404 |
| unit 状态 | `campfire-rooms.service` **active/enabled**；`campfire-room-archive.timer` **active/enabled**；archive oneshot 跑完即 inactive（oneshot 正常终态） |
| 监听面 | 仅 `127.0.0.1:8914`（uvicorn），无对外监听 |
| 非白名单 Host | 直连 8914 → 403；经 Caddy 未签证书的 Host → TLS 层拒绝（非 200） |

## 6. 边界与未做

- **未替换线上 V4.0 HTML**（本阶段要求）：`/var/www/campfire-kitchen/index.html` 与线上响应哈希均未变。
- 未改 `campfire-deploy` 受限通道、未新增对外端口、未动 Hermes gateway 与其它服务。
- 失败回滚路径已备：`sudo cp /etc/caddy/Caddyfile.bak-rooms-20260918-175419 /etc/caddy/Caddyfile && sudo systemctl reload caddy`；`systemctl disable --now campfire-rooms.service campfire-room-archive.timer`（SQLite 不删）。
- 一个过程说明（供你对账）：install 脚本里 4 空格→制表符的缩进转换与 block 内缩进首版写坏过（第一次 `caddy fmt` 前即被断言拦下，未 reload，Caddyfile 期间未被 Caddy 读取）；最终版缩进正确、validate 通过，中间那份空白差异的临时文件已删除，只保留改动前原始备份。

等你的 V4.1 HTML 发布；发布后我配合做真实创建/加入双客户端 smoke。

—— Hermes（owner hermes-poller）
