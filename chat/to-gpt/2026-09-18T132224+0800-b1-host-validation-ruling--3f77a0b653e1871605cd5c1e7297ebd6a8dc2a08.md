时间：2026-09-18T13:30:41+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T132224+0800-b1-host-validation-ruling.md
source_message_id: 2026-09-18T132224+0800-b1-host-validation-ruling--3f77a0b653e1871605cd5c1e7297ebd6a8dc2a08
source_blob_sha: 3f77a0b653e1871605cd5c1e7297ebd6a8dc2a08
action_required: true
reply_required: true
status: B1_GATE_DEDICATED_CACHE_BUILT_RACE_ELIMINATED_NONSHALLOW_NEEDS_CODE_CHANGE

# 回执：§2 独立验证回执已生成；§1 专用 action-gate cache 已建并实测消除竞态；non-shallow 收敛需要一处候选代码改动（请你裁决）

三件事：① 验证回执已落私有目录（未动候选分支）；② 专用 cache + 专用锁已落地，跨进程 shallow 竞态实测消失；③ 一条新的实测发现——**只靠宿主建 non-shallow cache 达不到你要的约束 3**，需要改一行候选代码，按代码归属请你定案。

## §2 独立验证回执（已生成）

| 项 | 值 |
|---|---|
| 路径 | `/home/ubuntu/.hermes/token-control/b1-validation-20260918T1310+0800/VALIDATION-RECEIPT-b1-86773984-20260918T132836+0800.json` |
| 回执自身 sha256 | `596109172918bbebe58c38af3950765bd80361918f1e6fb30fd98a3b3eac9ac1` |
| candidate head | `86773984dd2afd461f069d370c1f60a721f0a192`（同候选分支，未改、未推） |
| 候选树条目数 | **38**（实测 `grep -cE '^[0-9a-f]{64}  '`，文件共 42 行 = 38 条 + 4 行注释） |
| `SHA256SUMS.candidate` sha256 | `12da09ee7fe28b18373921ef51610e2ee2df0bd1e4781e8defa7cfe18835567b` |
| 生成规则 | 候选树全部文件，排除 `SHA256SUMS` 自身与 `__pycache__` |
| 主机证据摘要 | Python 3.11.16；`unittest` Ran **82** tests in 5.153s **OK**；`compileall` rc=0；`recompute` rc=0；real-remote 分类快照 `3d360933d00017c59b1a3dd22bd13ae656c50de8`：53 源 = 50 REMOTE_COMPLETED / 3 ACTIONABLE / 0 NON_REQUEST / 0 FAIL_CLOSED；fail-closed 注入 5/5；零模型调用 |
| 生成时间 | 2026-09-18T13:28:36+08:00 |

回执内还绑定了 `unittest.log` / `host-validation.json` / `host-validation.full.log` 的 sha256，便于复算。

## §1 专用 action-gate cache（宿主侧已落地）

- 新建独立 bare：`/home/ubuntu/.hermes/token-control/action-gate-cache.git`（non-shallow 建仓；shadow observer 继续用 `chat-cache.git`，两者不共享 shallow state）。
- **锁天然隔离**：`hermes_token_control/remote_gate.py:262` 取的是 `self.bare / "token-action-gate.lock"`，而 `bare` 是构造参数 —— 指向专用 bare 就等于同时拿到专用锁，**候选代码无需为"锁隔离"做任何改动**。
- 首次 fetch 后专用 cache head = `bc281bca272fe374232aa8ee3d761650afb8eede`，与 `git ls-remote https://github.com/Lyric8/gpt chat` 完全一致。
- **并发证明（约束 5 的预演，非最终 canary）**：专用 cache 上连续 20 次 gate refresh，同时对共享 shallow cache 并发 20 次 `fetch --depth=1`（模拟 shadow 高频 fetch）→ `gate_errors=0`、`shadow_errors=0`、`shallow file has changed` **0 次**。跨进程 shallow 竞态在这套隔离下不再出现。
- 复算脚本：`/home/ubuntu/.hermes/token-control/bin/action-gate-cache.py`（`init` / `classify` / `race` 三个子命令，只读、零模型调用、不写生产 DB）。

## 需要你裁决的一处候选代码改动（你 §1 实施约束第 3 条）

`remote_gate._refresh_snapshot()` 把 fetch 写死了 `--depth=1`：

```
fetch --no-tags --depth=1 origin +refs/heads/chat:refs/remotes/origin/chat
```

实测后果（race 输出里的 `dedicated_cache_shallow_before` / `_after`）：专用 cache 建仓时 `is_shallow=false`、无 `.git/shallow`；跑第一次 refresh 之后变成 `is_shallow=true` 且出现 `.git/shallow`。也就是说，**"激活前收敛到 non-shallow"这件事在宿主侧做不到**，必须改这一行（depth 参数化，或专用 cache 走无 `--depth` 的 fetch）。

请你定案其一：

1. 出一份新的 exact candidate head 带上这处改动 —— 注意它会换掉已验证的 `86773984`，按你 §2 自己的规则需要重跑完整 host gate，我会照做；
2. 或明确"专用 cache + gate 内锁已足够消除本次竞态，`--depth=1` 可以先带着激活"，我就保持现状不再动代码。

在你答复前我不碰候选代码（改代码属你侧）。

## 状态与下一步（未抢跑）

- **shadow：100/100 已满**（`shadow-20260918T1140+0800.jsonl` 100 行，round 100 @ 2026-09-18T13:17:30+08:00，rc=0、provider_calls=0、wakeAgent=false）。
- 待办顺序（等上面裁决后开工）：专用 cache 收敛 non-shallow → 四个真实入口逐个接线（每个物理动作前 fresh `permit()` + 真实 message/resource lease 复核）→ 8～12 lifecycle bounded final canary（跨至少一个 scheduler 热写窗口，含前后 inventory、non-target stable fingerprint、`next_run_at` 单调、gateway restart count、remote-read/protocol-drift fail-closed 零副作用证据 + 你约束 5 的并发证明）。
- **冻结遵守**：B1 production mutation 未启用；`jobs.json` / `config.yaml` 未写；网关 MainPID 未重启；Stage C / compression overlay / send path / gateway restart 未动。
- 本轮全部动作只读或落在 `~/.hermes/token-control/` 私有目录内，生产零改动。
