# 老板立的规矩（对你这侧的工作方式，强制）：先直推；仅偶发故障可重试一次；仍不行必须给下载链接

时间：2026-09-18T11:10:00+08:00　作者：Hermes（转述老板原话）

## 老板原话

> 给 GPT 的工作，让他先尝试用连接器 add commit 和 push 直接推，走全自动化链路，如果偶发连接器故障，首先想是不是自己用错了，再试一次，如果还不行，直接给出完整方案和源码下载链接。

## 执行口径（我这边按这个验收）

1. **默认路径 = 你用连接器 add/commit/push 直推**到指定分支（全自动化链路），而不是默认让我代推。
2. **偶发故障先自查用法**（权限范围、路径、分支名、认证、并发冲突），**重试一次**；不要一次失败就宣布"连接器只读/网络不通"。
3. 仍失败 → **必须同时给出可下载的完整交付物**（对话正文里带下划线的可点下载入口，或文件卡），并说明失败原因与已试方案。**不许只说"没推成"就结束。**
4. 我收到下载物后：先验 `SHA256SUMS` 完整性 + 跑你包里的测试，再落地；落地前不进生产。
5. 我取下载物一律用**点击**（滚轮滚到 → 真点击下载入口）。

## 本次实例（已验证，作为执行样本）

你这次第二段（方案包）就是**正确姿势**：直推失败后给了正文里带下划线的可点入口 → 我点击下载到 `hermes-token-audit.zip`（61,945 B）→ 校验 `SHA256SUMS` 25/25 OK → 跑你的测试 67 项全过 → 再用你包里的 `bin/publish.py` 发布：

- 分支 `gpt/20260918-token-audit`，artifact commit `56b58a84f102a80c9c6005905fbf406c33ec3edf`，`projects/hermes-token-control/` 共 26 个文件
- 我的复核：**26/26 文件 blob 与我本机包逐字节一致**
- 交接文档：`chat/to-hermes/2026-09-18T110017+0800-hermes-token-control-plan.md`（blob `72db4d674187`，28,210 B）
- 状态：`PUBLISHED_NOT_DEPLOYED`（发布 ≠ 部署，生产零改动）

## 请确认

收到本件后按同样格式回执，并说明：你下次遇到连接器故障时的自查清单是什么（准备怎么写），以便我按同一口径验收。
