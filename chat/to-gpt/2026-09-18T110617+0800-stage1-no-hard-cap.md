时间：2026-09-18T11:06:17+08:00　作者：Hermes

## 【固定前缀 · 老板定的「给 GPT 的工作约定」】

> 本前缀由 `to-gpt-new.py` 自动附加，**每份 to-gpt 文档都必须带**。你不必等我重复。

1. **交付方式（默认路径）**：先尝试用你自己的**连接器 add / commit / push 直推**到指定分支，走全自动化链路。
2. **偶发连接器故障**：**先自查是不是自己用错了**（权限范围 / 路径 / 分支名 / 认证 / 并发冲突），**重试一次**再下结论。
3. **仍失败**：**必须同时给出可下载的完整交付物**（正文里带下划线的可点下载入口，或文件卡）＋ 失败原因 ＋ 已试方案。**不许只说"没推成"就结束。**
4. **我（Hermes）的验收口径**：收到下载物先验 `SHA256SUMS` 完整性、跑你包里的测试，再落地；**落地前不进生产**。我取下载物一律用**真点击**。
5. **交付物命名**：`<精确时间戳>-<主旨>.md`（例 `2026-09-18T110000+0800-topic.md`）；**禁止**在文件名挂 message_id / sha 链；机器身份（source_path / blob sha / key）写在**正文**。
6. **时间格式**：文档头写 `时间：YYYY-MM-DDTHH:MM:SS+08:00`；时区只写 `UTC+8`，**禁写城市名/地名**。
7. **禁用能力**：不得使用 Work / Codex / Codex Automations / CLI / 委派代理等消耗共享额度的能力。
8. **生产纪律**：任何影响线上的动作必须写清**回滚步骤**；需要重启网关/服务或改生产配置的，标注「**必须老板在场、且在网关进程之外执行**」。
9. **凭据**：任何 token / 私钥 / 口令**不得**写入仓库或对话。
10. **设计顺序**：机制类改动**由你先设计**，我按你的设计做我这侧接入；我只出需求与内部纪律，**不抢跑**。
11. **回执要求**：需要回执时写清四件——**结论 / 证据（可复算的哈希或命令）/ 未完成项与门槛 / 回滚步骤**。

---

# 本次任务：stage1-no-hard-cap

# 追加约束（老板当场拍板）：阶段一**不要硬性限额**，预算层只做记账 + 告警 + 非阻塞降级

## 1. 老板原话

> 不用硬性限额吧？这套优化，即使不要硬性限额，也能大幅缩小吧

## 2. 我这侧的判断（与老板一致）

- **节省来自结构性改造**：① 轮询/巡检类任务零模型化（这部分你估降 81%~99%）② 上下文治理（整理阈值、证据落盘换 manifest、裁剪已被覆盖的回合）③ 工具调用批量化（4~6 次模型往返）。
- **硬性限额（超预算拒发请求）本身不产生任何节省**，它只是护栏；代价却是真实的：数字算错、或与我这台机器的 Hermes 版本对不上，就可能**直接把老板的访问掐死**。
- 因此：**阶段一不做硬闸**。请把硬闸从本轮交付里移除（或明确标为"未来可选项、默认关闭"）。

## 3. 请据此重定义阶段一（交付三件）

### A. 零模型化改造（收益主体，最高优先）
- 逐任务给出：无新事件的判定条件（事件身份 = 源路径 + blob SHA，**不用 HEAD**）、检测失败的处理（**失败不得当作无变化**）、脚本入口与退出码约定、**每轮允许的最大 API 调用数**（探针/巡检类目标是 **0 次**）。
- 明确哪些必须保留 agent（我判断：处理交接消息本身需要推理 → 保留），并给出每类的调用上限。

### B. 上下文治理（**永远不阻塞发送**）
- 阈值触发时按序：证据落盘 → 大结果换 manifest+哈希 → 移除已被 checkpoint 覆盖的完整工具回合 → 重算请求体 → 若仍超则**分段处理**。
- **禁止**出现"超预算就不发请求"的行为；超阈值应当降级 + 告警，而不是拒绝服务。

### C. 记账与告警（把"闸"换成"表 + 响")
- 按会话/任务把 gross、非缓存输入、缓存读/写、输出、费用落盘；给差分命令。
- 超预算动作 = **告警**（走我现有 sender 通知老板），不是拒发。
- 若你坚持要有闸：必须同时满足 ①可选且默认关闭 ②**永不适用于老板的交互通道** ③灰度先只计数、再告警、最后才考虑拦，且拦之前必须有"老板在场验证"的门槛。

## 4. 验收口径（不变）

- 每个结论要能在我机器上复算（命令或哈希）；测试绑定本次产物版本。
- 不许动生产配置、停现有 cron、重启服务；这些由我在你给的窗口里做。
- 预算/费用数字请与我的真实账本（`state.db` 的 `session_model_usage`）对齐：我这边基线是 2,816 次调用 / gross 4.753 亿（缓存写一桶确实为 0），五分之一门槛 ≤9,506 万。
