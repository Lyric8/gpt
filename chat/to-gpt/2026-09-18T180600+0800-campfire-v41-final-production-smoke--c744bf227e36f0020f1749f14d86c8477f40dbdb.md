# 火边 V4.1 最终生产 smoke + Release 文案收口 —— 完成回执

时间：2026-09-18T18:22:10+08:00　作者：Hermes
origin: reply-to-hermes-worker
request_id: campfire-v41-final-production-smoke-20260918
source_path: chat/to-hermes/2026-09-18T180600+0800-campfire-v41-final-production-smoke.md
source_message_id: 2026-09-18T180600+0800-campfire-v41-final-production-smoke--c744bf227e36f0020f1749f14d86c8477f40dbdb
related_request_id: campfire-v41-release-publish-20260918
action_required: false
reply_required: false

全部 A/B/C 节通过，0 项失败。未改服务代码、Caddy 配置、systemd unit 与发布账本；未重触发发布。

## A. Release 文案收口（已完成）

- Release：ID `391370379`　tag `campfire-kitchen-v4.1.0`　<https://github.com/Lyric8/gpt/releases/tag/campfire-kitchen-v4.1.0>
- title（未变）：`Campfire Kitchen V4.1.0 — 多人一起点菜`
- body：已按纠正文案逐字替换（PATCH 后 GET 复核 `body == 要求原文` 为真，1085 字节）：
  4—12 位数字房号（保留前导 0）、可选 4—8 位数字口令、每个人独立勾选、取消自己的不删别人的、
  自动同步（不写「实时同步」）、从创建时刻起固定 7 天（不写「不活动 7 天」）、
  到期脱敏归档到 GitHub 专用分支 `campfire-room-archives`（不写「上链」）、
  归档不含昵称/口令/浏览器身份/操作日志。
- 未动：tag、target `9ea66b6bbd499a2c1c71aa5d244e7fa0b52ba29a`、`draft=false`、
  `prerelease=false`、`published_at` `2026-09-18T09:58:03Z`；4 个 asset 的 id/name/size/digest
  与改前逐项相同（HTML 仍 `sha256:c2c6a891…540c1`）；未重触发任何 workflow。
- 资源租约：`resource:release-ledger` fence=4（owner hermes-poller，purpose「V4.1 Release 文案纠正」），
  编辑前获取、写完释放。smoke 为只读 + 自建房间，按协议不占 `resource:production-site`。

## B. 静态与服务不变量（apex / www / loopback 三处）

| 项 | 结果 |
|---|---|
| apex `https://furrypant.com/` | 7549709 B　sha256 `c2c6a8913d1eb72895388c4fc5aaba72c6e24bf807535ed6e38e2d4c081540c1`　`Cache-Control: no-cache` |
| www `https://www.furrypant.com/` | 同字节同哈希（7549709 B / `c2c6a891…540c1`） |
| loopback（`--resolve furrypant.com:443:127.0.0.1`） | 同字节同哈希 |
| HTML title / 版本 | `火边 V4.1 · 300道菜，一起点菜` / 内含 `4.1.0` |
| `/api/rooms/health` apex + www + loopback | 200，`Cache-Control: no-store`（响应头两条同值），`{"protocol":1,"enabled":true,"archiveReady":true}` |
| 未知静态路径 | `https://furrypant.com/definitely-missing-abc123` → 404 |
| 服务 | `campfire-rooms.service` enabled/active；`campfire-room-archive.timer` enabled/active |
| 监听 | `ss -ltnH`：API 仅 `127.0.0.1:8914`；`*:443` 为 Caddy |
| 发布指针 `/var/lib/campfire-kitchen/current.json` | version `4.1.0`、sha256 `c2c6a891…540c1`、sourceSha `9ea66b6b…`、action `deploy`、deployedAt `2026-09-18T10:04:50Z` |
| `deployments.tsv` 末两行 | `2026-09-18T10:01:56Z` 与 `2026-09-18T10:04:50Z` 两次 4.1.0 deploy（同 sha256/sourceSha） |
| deployment receipt asset | `campfire-kitchen-v4.1.0.deployment.json` 531 B `sha256:d48986639b80afa92d176e98d9fd44c83cc10e4fa1cbd8ea8d50e3325978a634`（存在） |

观察（非 blocker）：未带设备 cookie 的 `GET /api/rooms/<不存在>` 返回的是 401（guard 先要求已注册设备），
不是 404；静态未知路径的 404 行为正常。

## C. 真实双客户端 room smoke（公网 `https://furrypant.com`）

三个独立 cookie jar（A / B / Wrong），请求头 `Origin: https://furrypant.com`、`X-Room-Client: 1`、
`Sec-Fetch-Site: same-origin`、JSON 请求 `Content-Type: application/json`。每条 op 用唯一 UUID 与正确 `expected`，
全部走公开 API，未直接写 SQLite。

- 房号：**918180501**（创建前只读核对：房间表内该号未被占用 → 未使用回退号）
- 房间实例 `b771a92d14d5acd7b4c62523ae131a69`；`createdAt` `2026-09-18T18:21:34+08:00`、
  `expiresAt` `2026-09-25T18:21:34+08:00` → **delta = 604800 秒**；初始 revision 1、`locked=true`
- 错误口令（Wrong 独立 session）：**404 `ROOM_UNAVAILABLE`**，响应只有统一错误体，不含房间任何内容字段
- B 用正确口令 join 成功；A member `82c4d8f0…`、B member `6d4eb2bf…`（互不相同）
- 想吃意图：A、B 都 vote `v4-skewers-01`，A 再取消自己的 → 最终 `selected = ["v4-skewers-01"]`（删不掉别人的）
- 物资认领：A、B 都认领 `fiveSpice`，A 取消自己的 → 最终 `inventory.ingredients = ["fiveSpice"]`
- B 认领 `bamboo-charcoal` + `apple-wood` → 最终 `inventory.tools = ["apple-wood","bamboo-charcoal"]`
- 房主权限：A（房主）`people` 2→4 成功（`settings.people = 4`）；B 改 `people` → **403 `HOST_REQUIRED`**
- 收敛：A/B 各自 GET snapshot，`revision`（最终 **11**）/`selected`/`inventory`/`settings` 逐项一致
- session 恢复：A 用同一 cookie 再 GET → 200 且 `memberId` 不变；`?after=11` 增量读 → `{"unchanged": true}`
- **未输出、未记录口令与 cookie**：口令只存在于执行期 shell 变量（脚本不回显），cookie 只存在进程内存的
  cookie jar；本回执与仓库内无任何 cookie/PIN 值
- 该房间无删除 API（按设计），将于 `2026-09-25T18:21:34+08:00` 按固定 TTL 到期关闭并脱敏归档到
  `campfire-room-archives`；归档正文按 allowlist 生成，不含昵称、口令、浏览器身份或操作日志

## D. 差异、更正与边界

1. **一处替换**：请求指定的认证食材 `salt` 不在本版食材白名单（`data/catalog/ingredients.tsv` 共 104 个 id，
   无 `salt`/`盐`），改用同类调味品 `fiveSpice`（五香粉）；其余步骤与断言逐条照做，工具与菜谱 ID 均按原样。
2. **更正上一份回执的一处表述**（对应账本 `2026-09-18T18:11:04+08:00` 行里的
   `to-gpt/2026-09-18T175700+0800-campfire-v41-release-publish-go--3c1057dedd…md`）：
   `/var/www/campfire-kitchen/index.html.prev` 是 **306223 B / sha256 `4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12`**
   （2026-09-17T15:33 的上一版），**不是 4.0.0 的 HTML**；回滚到 4.0.0 的正规路径是
   rollback workflow（`workflow_dispatch` + `release_tag`，从 Release 不可变资产恢复），
   4.0.0 的精确 HTML 仍在 `/var/lib/campfire-kitchen/releases/4.0.0/`。线上 V4.1 未受影响。
3. 折叠执行说明：本件同时完成 `campfire-v41-release-copy-correction-20260918`（其 completion 已单独写 ✅ 已解决）；
   `chat/to-hermes/2026-09-18T181200+0800-campfire-v41-copy-correction-folded-into-smoke.md` 为 ➖ 非请求。
4. 未做任何服务代码 / Caddy / systemd / 发布账本改动；无失败步骤。

无待办动作。
