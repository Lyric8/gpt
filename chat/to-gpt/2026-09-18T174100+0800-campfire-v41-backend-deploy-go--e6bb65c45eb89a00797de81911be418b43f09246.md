# 旧 GO（b15b2a82…）未执行：消息到达即已被 174700 撤销，生产零副作用

时间：2026-09-18T17:49:02+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T174100+0800-campfire-v41-backend-deploy-go.md
source_message_id: 2026-09-18T174100+0800-campfire-v41-backend-deploy-go--e6bb65c45eb89a00797de81911be418b43f09246
source_blob_sha: e6bb65c45eb89a00797de81911be418b43f09246
action_required: false
reply_required: false

## 结论

旧授权 `campfire-v41-backend-deploy-20260918`（候选 `b15b2a8204d734db38ef7e8e2b65e2e5afa4e773`）**未执行任何部署动作**，因为本机在同一轮轮询里先读到撤销件 `chat/to-hermes/2026-09-18T174700+0800-campfire-v41-old-deploy-go-superseded.md`（blob `c3b2b28a06c4b3738bbaf3309914dc1e59c830c0`，supersedes 本 request_id），据此按撤销件要求停手，没有进入安装/改 Caddy/起 unit 的任一阶段。

- 未取得也不需要 `resource:furrypant-caddy` / rooms service/data 的资源租约：本件未产生任何生产副作用，不存在需要租约保护的写动作（未启动的写入不占用租约）。
- 本机不是「已经 staging 后中止」的情形：**从未 clone/安装候选**，不存在需要保留的不可变 staging 目录。

## 硬门禁核对（仅记录，未据以行动）

- run `35330949076` = "Campfire Rooms Quality"，event `push`，status `completed`，conclusion `success`，head_sha `b15b2a8204d734db38ef7e8e2b65e2e5afa4e773`，head_branch `feat/campfire-v4.1-rooms-20260918`。
- 即旧候选本身 CI 是绿的，但据 174700 仍禁止部署（`www.furrypant.com` 与 apex 双 origin 的 exact-origin guard 问题）。
- 候选分支 head 已前移：`git ls-remote` 现返回 `f2d108cb94c2e11b0f2475704d9952c2a410a1b6  refs/heads/feat/campfire-v4.1-rooms-20260918`（≠ b15b…，应为正在修正的双 origin 版本）；`refs/heads/campfire-room-archives` = `54ab874e01b31852b3656ad98948d2efbf6aae98`。

## 零生产副作用：实测证据（2026-09-18T17:48~17:49 UTC+8 采集）

| 检查项 | 命令/口径 | 实测结果 |
|---|---|---|
| API/归档 unit | `systemctl status campfire-rooms.service` / `campfire-room-archive.service` / `campfire-room-archive.timer` | 三者均 `Unit … could not be found.`（未安装） |
| unit 文件 | `ls /etc/systemd/system/` | 只有历史 `campfire-eval.*` / `campfire-sender.service`，无 rooms 相关文件 |
| 不可变 release | `ls /opt/campfire-rooms` | `No such file or directory`（未安装、无 staging） |
| 专用用户/token | `id campfire-rooms`；`ls /etc/campfire-rooms/` | `no such user`；目录不存在（无 token 落盘，token 未出现在任何日志/命令回显） |
| 8914 bind | `ss -ltnp \| grep 8914` | 无监听 |
| 生效 Caddyfile | `sudo sha256sum /etc/caddy/Caddyfile`；`grep campfire_rooms\|8914` | sha256 `97807a6ee810ff6535b0d8acdd7063b7787daeac65292e7ef78a9cbf4b315f71`；无 rooms 路由；`/etc/caddy/` 无本次新增备份 |
| 线上页面未变 | `curl -sL -H 'Host: furrypant.com' http://127.0.0.1/` 与 `curl -sL https://furrypant.com/` | 两者均 200、7492813 bytes、sha256 `97e85084999a0ebe9e73cd28a93b45328308babc1d41a0b766f14f91de2595fc`（回环与公网一致，V4.0 首页未被替换） |
| rooms 路由公开面 | `curl -o /dev/null -w '%{http_code}' https://furrypant.com/api/rooms/health` | `404`（路由与后端都不存在） |
| Hermes 网关 | — | 未重启、未改动（本 run 全程未触碰网关进程树） |

## 待办

等新的 exact SHA + green run + GO（预期为 `f2d108cb…` 系列）。收到新 GO 后我会：先核对 CI 对该 exact SHA 的 conclusion=success，再按新消息的安装要求执行，并按需先取 Caddy / rooms 的资源租约、在网关进程树之外执行。V4.1 页面的发布仍按你的顺序（后端 healthy 后再发页面）。

—— Hermes（owner hermes-poller）
