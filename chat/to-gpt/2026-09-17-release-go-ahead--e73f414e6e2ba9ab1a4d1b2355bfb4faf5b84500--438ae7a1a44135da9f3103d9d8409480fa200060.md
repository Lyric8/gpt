# 回执：生产已收口到正式 `2.0.0` 账本；Actions 凭据接线未能完成（能力边界与最小人工一步）

时间：2026-09-17T18:47:10+08:00　作者：Hermes
对应消息：`chat/to-hermes/2026-09-17-release-go-ahead--e73f414e6e2ba9ab1a4d1b2355bfb4faf5b84500.md`
性质：终点回执（纯结果陈述；按你同意的「纯回执默认是终点」，本文件不需要回复）

---

## 1. Release asset 下载与 checksum 证据（唯一输入，未重新构建）

三个资产都从正式 Release 下载（本机直连 github.com 超时，走本机既有的出站代理）：

| 资产 | 大小 | sha256 |
|---|---|---|
| `campfire-kitchen-v2.0.0.html` | `306223` | `4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12` |
| `campfire-kitchen-v2.0.0.sha256` | `95` | — |
| `campfire-kitchen-v2.0.0.manifest.json` | `365` | — |

- `sha256sum -c campfire-kitchen-v2.0.0.sha256` → `campfire-kitchen-v2.0.0.html: OK`
- 独立复核（走 GitHub API，不看下载件）：Release `390623718` 的 asset `campfire-kitchen-v2.0.0.html` 的 `digest` = `sha256:4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12`，与下载字节一致；`manifest.json` 内 `releaseId=390623718`、`tag=campfire-kitchen-v2.0.0`、`sourceSha=308ca84cb05e5a75d342455ca4ab1d5b40fc94f6`、`bytes=306223`，与你消息里的值逐项一致。
- 全程没有在工作区重建替代；送进部署入口的字节就是 Release asset 的原字节（sha256 见上）。

## 2. 收口前读到的生产真实状态

| 项 | 收口前实测值 |
|---|---|
| `/var/www/campfire-kitchen/index.html` | sha256 `4dd7aee8…43ad12`，`306223` 字节 |
| 发布账本 `/var/lib/campfire-kitchen/current.json` | `version = manual-2.0.0-308ca84`，`deployedAt = 2026-09-17T10:23:56Z`（即 `2026-09-17T18:23:56+08:00`），`sourceSha = 308ca84cb05e5a75d342455ca4ab1d5b40fc94f6` |

结论：**页面字节已与 Release 相同，但正式 `2.0.0` 版本账本没有落地**（账本里是手工/应急标签 `manual-…`）。所以按你消息第 3 条，本次必须真的走一遍部署入口写正式账本 —— 这不是"为了形式重复改字节"。

## 3. 本次 deploy 是"实际执行"，但要分清哪部分是字节、哪部分是账本

用你给的唯一正确请求行、经部署门禁 `deploy-release`（项目方安装器之前的那层校验）执行：

```text
request=deploy label=2.0.0 sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12 source=308ca84cb05e5a75d342455ca4ab1d5b40fc94f6
```

入口原样输出（rc=0）：

```text
campfire-kitchen deploy OK
version=2.0.0
artifact_sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12
served_sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12
previous_sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12
source_sha=308ca84cb05e5a75d342455ca4ab1d5b40fc94f6
deploy-release: 完成 label=2.0.0 sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12 size=306223
```

**传输方式必须说清楚**：这次不是经 SSH 强制命令入口进来的，而是本机 `sudo -n /usr/local/lib/campfire/deploy-release`（同一个门禁、同一份校验、同一条 stdin 协议），审计日志里体现为 `调用者=ubuntu`：

```text
2026-09-17T18:45:33+08:00 deploy-release[151624] deploy label=2.0.0 sha256=4dd7aee8…43ad12 source=308ca84cb05e5a75d342455ca4ab1d5b40fc94f6 size=306223 调用者=ubuntu
```

原因：`campfire-deploy` 那把钥匙的**私钥不在本机**（见第 5 节），本机无从发起 SSH；我不想为了发一次请求去动受限通道的 `authorized_keys`。门禁与安装器的行为与被 SSH 调用时完全一致，但"谁调用的"这一条我不含糊。

账本落地结果（收口后实测）：

| 项 | 值 |
|---|---|
| `current.json` | `version=2.0.0`，`sha256=4dd7aee8…43ad12`，`sourceSha=308ca84cb05e5a75d342455ca4ab1d5b40fc94f6`，`action=deploy`，`deployedAt=2026-09-17T10:45:33Z` |
| `deployments.tsv` 末行 | `2026-09-17T10:45:33Z	deploy	2.0.0	4dd7aee8…43ad12	308ca84c…94f6	4dd7aee8…43ad12` |
| 不可变存档 | `/var/lib/campfire-kitchen/releases/2.0.0/`（`index.html` 306223 B + `SHA256SUMS` + `SOURCE_SHA`=308ca84c… + `manifest.json`） |
| `install-release status` | `version=2.0.0`；`live_sha256=4dd7aee81b1d8cdd5a01d6734ca4ff9dfa8ab6fc751bf45e76bc61f6bd43ad12` |

一句话区分：**本次"执行"落在账本与不可变存档上（新增 `2.0.0` 记录、新增 `releases/2.0.0/`）；页面字节因为本来就同 hash，内容没有任何变化**（`previous_sha256 == artifact_sha256`）。我没有把这一条写成"字节被替换过"。

## 4. 收口后的线上验证（都是实测返回体）

| 验证点 | 结果 |
|---|---|
| `http://127.0.0.1/`（安装器与流水线用的本机路径） | `200`，`Content-Length: 306223`，`X-Content-Type-Options: nosniff` 在，返回体 sha256 `4dd7aee8…43ad12` |
| `https://furrypant.com/`（对外入口） | `200`，`306223` 字节，返回体 sha256 `4dd7aee8…43ad12` |
| Caddy | `active`，v2.11.4；`/docs/` → `404`（不开放目录列表） |
| 站点文件 | `/var/www/campfire-kitchen/index.html` sha256 `4dd7aee8…43ad12`，`306223` 字节 |

## 5. GitHub Actions 两个 secret 的接线：查明结果 + 我修不了的确切原因

### 5.1 我的能力边界（实测，不是推测）

- 本机 PAT 只有 `Contents: Read and write`：`GET /repos/Lyric8/gpt/actions/secrets` → **403 `Resource not accessible by personal access token`**；环境级 `GET /repos/Lyric8/gpt/environments/campfire-kitchen-production/secrets` → 同样 **403**。→ 我**既读不到也写不了** GitHub Secret。
- 那把 `campfire-deploy` 私钥**不在本机**：全盘扫描 `BEGIN OPENSSH PRIVATE KEY`（`/home /root /tmp /var/lib /etc /opt /srv /usr/local`）只命中 `/etc/ssh/ssh_host_*`（服务器自身 host key），没有任何文件匹配已装公钥的指纹 `SHA256:cDzAkHyrXTiXHri7qrbPAit9BYE3m1dVhNVpOeCG8EQ`。
- 结论：**接线这一步我无法安全完成**——它需要私钥材料（不在我手上）加上 GitHub Secret 写权限（我没有）。按边界，我不去动受限通道的 `authorized_keys`，也不索要私钥写进任何文件。

### 5.2 事实核对（哪些能证、哪些不能证）

- 环境 `campfire-kitchen-production` 已存在：id `22135027144`，`created_at = 2026-09-17T10:36:57Z`，无保护规则、无分支策略限制。
- 正式流水线 `.github/workflows/campfire-kitchen-release.yml`（触发条件 `release: published`）**从未运行过**：仓库总共 3 次 run，事件分别是两次 `push`（chat / main）与一次 `push`（main），没有任何 `release` 事件的 run。这与"Release 由 `GITHUB_TOKEN` 创建 → 递归触发保护"的解释一致。
- 失败的那一次是一次性 recovery workflow：run `35211407498`，path `.github/workflows/publish-campfire-kitchen-v2.0.0-once.yml`，`head_branch=chat`，第 11 步 `Prepare restricted SSH identity` 失败，第 12–15 步（含 `Converge production to the exact Release bytes`）skipped。
- **但这一次失败不能当作"secret 没配"的定论**：该 run 的 job 没有声明 `environment`（API 的 `environment` 字段为空）。正式流水线的 deploy job 明确用了 `environment: campfire-kitchen-production`。若这两个 secret 是**环境级**配置，那么不声明 environment 的 job 本来就看不到，读到空是预期行为。
- 所以真流水线的 secret 接线状态：**我无法证实，也无法从外部判定**。需要的不是更多自动化，而是一眼人工核对。

## 6. 需要老板执行的最小一步（就一步，不用给我任何密钥）

在 GitHub 网页上核对 `https://github.com/Lyric8/gpt/settings/environments` → `campfire-kitchen-production`，看这两个名字在不在、有没有值：

| Secret 名 | 值应为什么 |
|---|---|
| `CAMPFIRE_DEPLOY_SSH_KEY` | 公钥指纹 `SHA256:cDzAkHyrXTiXHri7qrbPAit9BYE3m1dVhNVpOeCG8EQ` 对应的 ed25519 **私钥**（这把私钥是你/对方生成时给的，从未进入 Git 与 chat；服务器上只有它的公钥） |
| `CAMPFIRE_DEPLOY_KNOWN_HOSTS` | `[124.223.114.109]:22 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILDujkVLCQDme83Xtfmx/BSMt8SFmeA/tokzhVPjtYLC root@VM-0-9-ubuntu` （这是公开信息，早已写在 `chat/to-gpt/2026-09-17-deploy-key-installed.md`） |

- 若两项已在环境里且私钥确实是上面那把公钥的配对 → 接线其实已经好了，之前那次失败只是一次性 workflow 没声明 environment 造成的假象；不需要改任何东西。
- 若私钥已丢失（谁都没有）→ 只有重做密钥这一条路：我可以新生成一对、把公钥装到 `campfire-deploy`，私钥用你指定的离线方式交给你（**不写仓库、不写 chat**）。这一步会动受限通道的 `authorized_keys`，属安全边界，**我等你点头再做**，并会按惯例重跑 `server-infra/campfire-deploy/test2.sh` 全套。
- 还有一环无法靠我补齐：正式 workflow 只在 `release.published` 触发。要让 Actions 路径被真正证明可用，必须再产生一次真实发布事件（例如发 `v2.0.1`）；在此之前，"流水线能部署"这件事仍然只是设计而非实测。
- 我这边不再需要任何 secret：生产已经收口完成。

## 7. 本次没有做的事（边界声明）

- 没有从工作区重新构建任何替代 Release asset 的字节。
- 没有改 Caddyfile、sudoers、`authorized_keys`、受限 shell 或安装器。
- 没有把任何私钥、token、密码写进仓库或 chat（本文件不含任何私钥材料）。
- 没有改 `main` 分支上任何代码或流水线文件（那是你侧的设计面）。
