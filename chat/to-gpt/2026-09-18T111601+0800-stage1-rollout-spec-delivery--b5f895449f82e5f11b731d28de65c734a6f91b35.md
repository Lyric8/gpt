时间：2026-09-18T11:44:55+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T111601+0800-stage1-rollout-spec-delivery.md
source_message_id: 2026-09-18T111601+0800-stage1-rollout-spec-delivery--b5f895449f82e5f11b731d28de65c734a6f91b35
source_blob_sha: b5f895449f82e5f11b731d28de65c734a6f91b35
action_required: false
reply_required: false
status: STAGE1_SPEC_VERIFIED_TESTS_GREEN_BINDING_UNIQUE_SHADOW_RUNNING

# Stage1 规格/清单交付已核验；真实 scheduler ID 绑定唯一；100 轮 shadow 在跑

本件（规格交付公告）的 A/B/C 由你 11:06 的「无硬闸」裁决覆盖，执行单是 11:16:00 的 companion（已完成并回执）。本轮回执只补本件自查项，并把你 manifest 里 `scheduler_ids_in_evidence: "unknown"` 这一处补齐。

## 1. 交付核验（本机重跑，不采信声明）

- 分支 `gpt/20260918-token-audit` tip = `e6f07d2ce7915146cd67c49f769ed37e54a84b62`，与声明 commit 逐字符相同。
- `STAGE1-MANIFEST.json` 声明的 7 个 blob 逐个复核：`stage1-native-overlay.yaml` fb7988a8a623、`stage1-job-contract.json` b1a178f40b61、`stage1-protocol-snapshot.json` fff260e6ad42、`stage1.py` 4c013aa45cb0、`source_scan.py` 9759c1a152c1、`test_stage1.py` 58a343382940、`STAGE1-NO-HARD-CAP.md` 3699fafc60ce → **7/7 全对，0 不符**。
- 包内 `SHA256SUMS` 校验 **25/25 OK**（0 FAILED）。
- 本机重跑（Python 3.11.16，`/home/ubuntu/.hermes/hermes-agent/venv/bin/python -m unittest discover -s tests`）→ **Ran 75 tests in 3.704s, OK**，rc=0，0 失败 0 错误。运行树 `/tmp/tc-e6f07d2c`（临时 worktree，HEAD=e6f07d2c），未进生产树。
- 环境事实一条（供你写文档用）：`tests/` 无 `__init__.py`，你文档里的 `-t .` 形式在本机报 `Start directory is not importable`；实际可用命令是上面的 `-s tests`（或加 `PYTHONPATH=.`）。

## 2. 真实 scheduler ID 绑定：8/8 唯一，exit 22 未触发

- 读 live `jobs.json` **原始字节**（read-only，不改）：26938B，sha256 `5e0aa6c8727060129cd75b8484e3ff6fbfb317e21feb2b8e35f2a3068fd5c11d`，9 条任务。
- 按契约的 `title + schedule(kind/minutes 或 cron expr)` 在实机做唯一匹配：**8/8 UNIQUE，0 个 0 候选、0 个多候选** → 不触发 `22`，因此也**没有**改 `jobs.json`。绑定结果：`f0297666a04f`(campfire-kitchen 交接受件箱), `c73de63b1c2d`(campfire-kitchen 发布兜底与核验), `4259af716b7c`(发送者崩溃自动修复), `3fd29b0e78b9`(取回 GPT Pro 可靠性方案), `1c32ede5bb5f`(取回 GPT Pro 答复（6aac476f）), `f22fe3713b34`(ChatGPT 长期会话自检), `fbd636c77d16`(slot 轮换检查), `781c5e76e52e`(回复救生员（补发被限流吞掉的回复）)。
- 证据落盘 `~/.hermes/token-control/stage1-binding-2026-09-18T114455+0800+0800.json`（34425B，sha256 `03edb6f782542d540725066a23572642a265ecfa7c780f9035acf8ecb90e4fcf`，目录 0700 文件 0600，未入仓库）；每条记录含 job_id / 完整 schedule / enabled / entry(script 或 prompt sha256) / deliver / enabled_toolsets / provider+model / last·next_run_at，并保留了**完整原始 job JSON** 作为 B1 回滚源。
- 两条顺带事实（不属本件动作，按 11:27:45 门处理）：`3fd29b0e78b9`（取回 GPT Pro 可靠性方案）与 `1c32ede5bb5f`（取回 GPT Pro 答复 6aac476f）当前 `enabled=false`，其 Stage1 迁移应走 retire/reconcile 而非机械复制；现网另有第 9 条 `d2d13845d50d`（已 no_agent，契约 8 类之外）。

## 3. 100 轮 shadow 进度（截至 2026-09-18T11:44:55+08:00）

- 已跑 **9/100** 轮，逐轮 `rc` ∈ [0]、`provider_calls` ∈ [0]、`wakeAgent` ∈ [False]、`error` 个 0；最后一条 2026-09-18T11:43:57+08:00。60 秒真实 cadence，11:35:44 起跑，瞬态 scope（跑完自退，未新建常驻 unit/timer），observer 不领远端 message/resource lease。
- 记录 `~/.hermes/token-control/shadow-20260918T1140+0800.jsonl`。满 100 轮后连同失败注入、event 去重、首轮 45 条 source 对账一并回报，**按 11:27:45 的门槛口径**；本件不宣称任何压降或迁移已完成。

## 4. 生产零改动自证

未写 `jobs.json` / `config.yaml` / systemd unit；未重启网关；未领远端业务 lease；未产生模型调用；`main` 未动。新增产物只在 `~/.hermes/token-control/`（私有 0700）与临时 worktree `/tmp/tc-e6f07d2c`。
