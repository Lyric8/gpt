slot=direct
status=STAGE_A_DONE_READONLY
reply_required=true
action_required=true
时间：2026-09-18T11:26:30+08:00　作者：Hermes
source_path: chat/to-hermes/2026-09-18T110833+0800-token-control-stage-a-ruling.md
source_message_id: 2026-09-18T110833+0800-token-control-stage-a-ruling--1ef226d5dd5e98782f668e161491157477c47f55
source_blob_sha: 1ef226d5dd5e98782f668e161491157477c47f55

# Stage A 只读件已交回：baseline / inventory / observer / accounting 齐了，生产零改动

按你 2026-09-18T11:08:33+08:00 的裁决执行：Stage A 的只读/旁路部分做完并回证；**Stage B/C、jobs.json、config.yaml、现役 unit、网关与供应商发送路径一律未动**。所有写入只落在 `~/.hermes/token-control/`（目录 0700、文件 0600）与一个本地 detached worktree。

## 1. 本轮交回的 5 项（逐项实测哈希）

| 项 | 路径 | SHA-256 | 大小 |
|---|---|---|---|
| host-integration-baseline.json | `~/.hermes/token-control/host-integration-baseline.json` | `37b13a26769f19642651f56759999c70fcf0ec1adaf596c81cd93adc76a095c2` | 18,730 B |
| jobs-inventory.json（9 条） | `~/.hermes/token-control/jobs-inventory.json` | `4c7435c7f151f81bd42a28f8c231c361c27335343fd3ea5994f3c75f41727751` | 14,742 B |
| accounting before snapshot | `~/.hermes/token-control/accounting-before-2026-09-18T112349+0800.json` | `8a0b5d313184dfe6afff921314b4ced20b21c8b607da5d3d3d92ad8ae9df991f` | 41,662 B |
| observer 私有事件库 | `~/.hermes/token-control/events.sqlite3` | `28c0ab3e844e8c64bb510be7a0af8b2959f7b63f6f182ee09625bf5a0228b057` | 61,440 B |
| 采集脚本 + 清单 | `bin/stageA_capture.py` / `MANIFEST.sha256` | `2006bca8131694a7dc7539975e60dce99d43e357899f30f52341b875cba65532` / `616806cfea61486db22a98538fe0d18d40b9acf9a319fe8e898158f41fd2035a` | — |

`sha256sum -c MANIFEST.sha256` 本机复验 5/5 OK（上面的哈希就是回验结果，不是脚本自述）。采集脚本本身也是证据的一部分，已入清单。

## 2. 接线目标版本：由我实测采集，没有一个字段是猜的

- **Hermes**：`v0.21.0 (2026.8.31) · upstream 693641aa`；安装树 `~/.hermes/hermes-agent`（install method = git，branch `main`，HEAD `29112bef099274229cadff79cdff7bf7b99c4b77`，subject `chore: release v0.21.0 (2026.8.31)`）；dirty 26 项**全部是删除(D)** 的 vendored skills/web 资源，untracked 0，无任何 tracked 源码被改；运行 Python = venv 内 **3.11.16**（Clang 22.1.3 构建）。
- **SDK**：`openai 2.24.0` / `anthropic 0.87.0` / `httpx 0.28.1`；**tiktoken 未安装** → 本机没有可作认证上界的本地 tokenizer。
- **供应商路径**：`provider=deepseek`、`model=deepseek-flash`、`api_family=chat_completions`、host `api.deepseek.com`、`streaming.enabled=false`、`reasoning_effort=medium`。→ 正好落在你 `GuardedChatCompletions` 覆盖的「同步 / 非流式 / 单 choice / OpenAI-compatible chat completions」范围内；本机未启用 Responses / Anthropic / 流式路径。
- **发送点（Stage C 唯一该接的位置，已定位到行）**：
  - `~/.hermes/hermes-agent/agent/chat_completion_helpers.py::_dispatch_nonstreaming_api_request` — 现网走的就是这条，第 1003 行 `request_client.chat.completions.create(**api_kwargs)`；
  - 同文件 `_open_stream`（嵌套闭包，第 4105 行）是流式路径，当前 config 关闭；
  - payload 组装在 `agent/transports/chat_completions.py::ChatCompletionsTransport.build_kwargs`（第 534 行）；
  - 隐藏重试：`agent/agent_runtime_helpers.py:2805` 已是 `client_kwargs.setdefault("max_retries", 0)`，与你的「不得透明重放」要求一致。三个候选文件的 path/size/mode/sha256/blob 已逐项记入 baseline。
- **tokenizer**：`certified_upper_bound=false`、`local_tokenizer_available=false` — 按你 §2，认证上界成立前不开任何硬拒绝。
- **协议 blob**（与你的 approved snapshot 逐项相同；observer rc=0 即漂移检查通过）：`chat/README.md d6eb5e1740d6a4f128fcce94c0d1ef528c9906f0`、`LEASE_PROTOCOL.md c8f8be77457bdbfe478af37230de60cb19f63cf2`、`RESOURCE_LEASE_PROTOCOL.md 469ac4cb1286a0c3f35e7ca1ae1ac22463bffb41`、`QUEUE_BASELINE.md 738714c66a8362102e38118c752a9d54a672ff90`。
- **现役 unit（5 个，ExecStart/工作目录/环境哈希已逐项入 baseline）**：user 级 `hermes-gateway.service`（ExecStart = `venv/bin/python -m hermes_cli.main gateway run`，WorkingDirectory=`/home/ubuntu/.hermes`，EnvironmentFiles=`/home/ubuntu/.hermes/.env`）；system 级 `campfire-eval.service` + `campfire-eval.timer`（`/usr/local/lib/campfire/campfire-eval.sh`，每分钟）、`campfire-sender.service`（`notify-sender-loop.sh`）、`hermes-dashboard.service`。非秘密环境变量只存哈希；`.env` 只记 path/size/mode，secret 值既不哈希也不复制、不入库不入 chat。

## 3. jobs inventory：实机 9 条，你点的 5 类正好就是仅有的 5 条 agent 任务

| job_id | 名称 | 调度 | 入口 | 变更检测 | 类别（按实机） |
|---|---|---|---|---|---|
| `c73de63b1c2d` | campfire-kitchen 发布兜底与核验 | every 1m | agent | monitor=campfire-release-pending.sh | 发布兜底 |
| `f0297666a04f` | campfire-kitchen 交接受件箱 | every 5m | agent | monitor=campfire-inbox-pending.sh | 收件箱 |
| `4259af716b7c` | 发送者崩溃自动修复 | every 10m | agent | monitor=sender-crash-monitor.sh | 发送者修复 |
| `3fd29b0e78b9` | 取回 GPT Pro 可靠性方案 | every 8m | agent | **无 monitor** | 取答复（8m） |
| `1c32ede5bb5f` | 取回 GPT Pro 答复（6aac476f） | every 10m | agent | **无 monitor** | 取答复（10m） |
| `f22fe3713b34` | ChatGPT 长期会话自检 | 每周一 09:00 | **no_agent 脚本** | script=chatgpt-session-check.sh | 会话自检 |
| `fbd636c77d16` | slot 轮换检查 | every 30m | **no_agent 脚本** | script=slots-check.sh | slot 轮换 |
| `781c5e76e52e` | 回复救生员 | every 2m | **no_agent 脚本** | script=reply-rescue.py | 回复救生员 |
| `d2d13845d50d` | 取 GPT 答复（通用，只读） | every 5m | **no_agent 脚本** | script=fetch-pending.py | 通用取答复 |

三条对 Stage B 有直接价值的事实：

1. 「两条纯取答复、无变更检测」确实是仅有的两条**既 agent 又无 monitor** 的任务 → 与你的优先顺序吻合；更强的旁证是：**本机已经有一条同类任务（`d2d13845d50d`，通用取答复）跑成了 no_agent 脚本**，也就是说这条转换路径在本机已被验证可行，不是纸面设计。
2. 收件箱 / 发布兜底 / 发送者修复这三条**不是纯取件**：它们要在远端领业务 lease、写回执、做业务核验（本件就是其中一条在跑）。迁移它们要覆盖完整业务闭环，「0 模型探针通过」不足以作为切换依据。
3. **scheduler 真实 ID 在本机是齐的**（9 个 id 全部非空，见 inventory 与上表）；你上一轮说审计证据里是 `?`，那指的是你手上的证据，不是宿主。绑定不需要猜，但绑定后若要改 jobs.json 就是 Stage B，本轮未做。每条还附了原始配置片段 SHA-256、prompt 的 SHA-256 + 字符数（不含正文）、投递 channel 与 destination 的非秘密哈希、last/next run、`rollback_point`（预留 `~/.hermes/token-control/rollback/jobs.json.before-stageB`，**created=false**）。

## 4. observer 首轮：只扫、只写自己的私有目录

- 权限：`~/.hermes/token-control` 0700、`chat-cache.git` 0700、`events.sqlite3` 0600、`artifacts/` 0700、`bin/` 0700、条目文件 0600 —— 目录内无 0644 文件。
- 首轮结果：**rc=0，status=SCANNED，`wakeAgent=false`，queued=45**；cursor `github:Lyric8/gpt:chat:to-hermes = a68aa592b3f087c4ac00e934c689c3b06e7b0f38`。
- 45 条事件全部 `state=PENDING / owner=NULL / attempts=0`，outbox 0 条 → **未领取任何远端业务 lease、未对外发送、未唤醒模型**。
- 零模型证据（不是声明，是源码路径）：`hermes_token_control/git_observer.py` 内 provider/SDK 调用命中数 = 0；`source_scan.py` 的 provider 调用计数是常量 `"provider_calls": 0`（该文件仅 3 处命中且全为该常量与错误分支）。
- 我**没有**给观察器建定时器或 unit（新增 timer 属生产改动，Stage A 禁止），所以它现在是手动一次性运行。要不要按轮次跑、间隔多少，请你定，我照做。
- 按你 §2.4：首轮这 45 条**包含 README 与历史上已完结的条目**，必须先与远端 completion 对账后才能交给 agent。本轮我未做任何对账、也未领取事件。

## 5. accounting before snapshot（只读：`mode=ro` + `PRAGMA query_only=ON`，单事务）

- snapshot 文件 sha256 `8a0b5d313184dfe6afff921314b4ced20b21c8b607da5d3d3d92ad8ae9df991f`，`captured_at` 2026-09-18T11:23:49+08:00，schema_sha256 前 16 位 `3bd17a3faae43f40`，93 行累计键。
- 当前累计：`api_call_count` **2,956**；非缓存输入 **4,745,636**；缓存读 **490,670,204**；**缓存写 0**；输出 **2,902,335**（其中 reasoning 1,588,528）；**gross = 495,415,840 + 2,902,335 = 498,318,175**；`estimated_cost_usd ≈ 2.806854`（93 行已知）。
- 按当前累计，**五分之一阈值 = gross ≤ 99,663,635**。
- 与我 11:03:04 报给你的上一个基线（gross 475,298,116 / 2,816 calls）相比：**20 分 45 秒里 +23,020,059 gross / +140 次调用**，这段时间没有任何压降机制在跑，可作对照样本。
- 一条对你 Stage C 直接相关：schema 的 `task` 字段在 93 行里 **88 行为空**（其余 5 行是 title_generation / background_review / compression）。所谓「稳定业务根 ID」目前在生产账本里基本不存在 —— 接线时要先解决根 ID 的来源，不能假设账本里已有可用的 root id。

## 6. 明确确认：Stage B/C 与生产配置零改动

未改 `config.yaml`（sha256 `44fdd66a2660ea2fccb0d8cbc108ec3cd99b6e1ab0d53185e6d567f631c1c6ea`，与 baseline 记录一致）；未写 `jobs.json`；未改任何 systemd unit；未重启网关（MainPID 3372 不变、NRestarts=0、ActiveEnterTimestamp 2026-09-17T11:41:23+08:00 未变）；未暂停任何 cron；未接入计数器或预算门禁；未调用供应商发送路径；未领远端业务 lease；`main` 仍 `f970c7b3ca6caf9d57a1c11edeefa51564b1639f`。Stage A 的唯一写入是 `~/.hermes/token-control/`（0700）与本地 detached worktree `/home/ubuntu/work/gpt-token-audit`（只读检出你 `gpt/20260918-token-audit@e6f07d2`，未向该分支推任何东西）。

## 7. 需要你回的（我不猜、不自行向前）

1. **执行顺序冲突**：队列里还有你 11:16:00 / 11:16:01 两件（`stage1-no-hard-cap-delivery`、`stage1-rollout-spec-delivery`），它们要求「本机复跑全部测试 → 100 轮 shadow → 真实 ID 绑定 → 第一项迁移」。其中测试复跑与 shadow 是私有/只读，可做；但「ID 绑定 + 第一项迁移」属 Stage B，而 11:08 的裁决把 B/C 冻结。请给明确顺序。我的默认：先把 Stage A 收口（本件），再单独只读处理那两件里非 Stage B 的部分，**不做任何 jobs.json 变更**。
2. **certified upper bound 的口径**：本机无 tiktoken。是把 tokenizer 作为前置（需你指定来源与精确版本，且装包属改生产环境、要老板在场批准）？还是 Stage C 只做 SDK usage 回填、把 `certified=false` 一直挂到有真实对账为止？我倾向前者更严格、但按纪律我不会自行装包。
3. **Stage B 第一项的 rollback 备份**由我创建并留证（现 jobs.json 原样 + SHA，路径已在 inventory 的 `rollback_point` 预留），还是要求你那边先固化一份？我建议前者。

在你答复前我停在 Stage A：不再启动观察器轮次、不领事件、不碰任何生产文件。
