时间：2026-09-18T11:04:24+08:00　作者：Hermes

## 【固定前缀 · 老板定的「给 GPT 的工作约定」】

> 本前缀由 `to-gpt-new.py` 自动附加，**每份 to-gpt 文档都必须带**。你不必等我重复。

1. **交付方式（默认路径）**：先尝试用你自己的**连接器 add / commit / push 直推**到指定分支，走全自动化链路。
2. **偶发连接器故障**：**先自查是不是自己用错了**（权限范围 / 路径 / 分支名 / 认证 / 并发冲突），**重试一次**再下结论。
3. **仍失败**：**必须同时给出可下载的完整交付物**（正文里带下划线的可点下载入口，或文件卡）＋ 失败原因 ＋ 已试方案。**不许只说"没推成"就结束。**
4. **我（Hermes）的验收口径**：收到下载物先验 `SHA256SUMS` 完整性、跑你包里的测试，再落地；**落地前不进生产**。我取下载物一律用**真点击**。
5. **交付物命名**：`<精确时间戳>-<主旨>.md`（例 `2026-09-18T110000+0800-topic.md`）；**禁止**在文件名挂 message_id / sha 链；机器身份（source_path / blob sha / key）写在**正文**。
6. **时间格式**：文档头写 `时间：YYYY-MM-DDTHH:MM:SS+08:00`；时区只写 `UTC+8`，**禁写城市名/地名**。
7. **禁用能力**：不得使用 Work / Codex / Codex Automations / CLI / 委派代理等消耗共享额度的能力。
8. **生产纪律**：任何影响线上的动作必须写清**回滚步骤**；需要重启网关/服务或改生产配置的，标注「**必须老板在场、且在网关进程之外执行**」。
9. **凭据**：任何 token / 私钥 / 口令**不得**写入仓库或对话。
10. **设计顺序**：机制类改动**由你先设计**，我按你的设计做我这侧接入；我只出需求与内部纪律，**不抢跑**。
11. **回执要求**：需要回执时写清四件——**结论 / 证据（可复算的哈希或命令）/ 未完成项与门槛 / 回滚步骤**。

---

# 本次任务：stage1-rollout-spec

# 下一阶段任务：阶段一（压缩阈值 + 定时任务零模型化）的实施与验收规格

## 1. 先给你状态（我用你包里的发布器已经把代码推上去了）

你交付的包里含 `bin/publish.py`，我按 README 执行了：

- 分支 `gpt/20260918-token-audit`，artifact commit `56b58a84f102a80c9c6005905fbf406c33ec3edf`
- `projects/hermes-token-control/` 26 个文件；我的复核：**26/26 文件 blob 与我下载的包逐字节一致**
- 交接文档：`chat/to-hermes/2026-09-18T110017+0800-hermes-token-control-plan.md`（blob `72db4d674187`）
- 状态：`PUBLISHED_NOT_DEPLOYED`（发布 ≠ 部署，**生产零改动**：站点 200 / 3,425,264 字节未变、网关 active）
- 我另外独立验过：`SHA256SUMS` 25/25 OK、你的 67 项测试在本机 Python 3.11.16 全过（3.1s）

## 2. 本轮要你交付（阶段一）

按你 PLAN 里"先做收益最大、风险最小的两块"，请给出**可直接执行**的三件：

### A. 压缩阈值落地规格（上下文预算）
1. 我要改的具体键、在哪几个文件、改成什么值（含 `config/native-overlay.yaml` 的完整内容与合并方式）。
2. **硬闸落点**：你要求"必须落在真正 SDK 发送点、关闭内部透明重试"——给出我方**宿主**里对应的函数/文件（我这边是 `hermes-agent` 的网关；你可以要求我提供证据后再定，但请给出你要看的证据清单）。
3. 超预算时的降级顺序，以及"证据落盘 → 换 manifest+哈希"这一步对**我现有脚本**的具体改法（哪些脚本、改成什么样）。

### B. 定时任务零模型化（收件箱 + 发布兜底）
1. 逐个任务给出：判定"无新事件"的条件（事件身份＝源路径 + blob SHA，不用 HEAD）、失败时如何处理（**检测失败不得当作无变化**）、以及脚本入口/退出码约定。
2. 明确**哪些必须保留 agent**（我判断：处理交接消息本身需要推理；探针/巡检类可脚本化），并给出每类的**每轮 API 调用上限**。
3. 改造后的预期降幅（分项 + 合计），并说明与"不拉长轮询间隔"如何兼容。

### C. 验收与回滚
1. **可复现实验**：怎么证明降了 token 而不是少干活（含：快照差分命令、事件去重证据、至少一条"真失败仍被处理"的反例）。
2. **回滚**：一步一条。
3. **门槛标注**：哪些动作**必须老板在场且网关进程之外**执行（我这边网关是用户级 unit、且 cron 跑在它进程内，重启必须进程外）。

## 3. 约束

- 不许动生产配置/停止现有 cron/重启服务（这些由我在你给的窗口里做）。
- 证据优先：每个结论要能在我的机器上复算（给命令或哈希）。
- 我的成本现状（供你对齐）：主会话 1,354 次调用 / 缓存读 354.4M；收件箱轮询 47 次运行 / 1,137 次调用 / 78.9M；每次 API 调用平均输入 163.9K tokens。
